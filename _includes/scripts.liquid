<script>
    {% if page.hasForm %}
        {% comment %}
            Honeypot URL form field handling
        {% endcomment %}

        document.addEventListener('DOMContentLoaded', () => {
            // fieldWeb: clear anything a password manager or browser autofilled before ignore-hints are applied.
            const webField = document.getElementById('field-website');
            if (webField) webField.value = '';
        });
    {% endif %}
</script>
