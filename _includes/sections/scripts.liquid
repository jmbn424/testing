<script>
    {% if page.hasForm %}
    document.addEventListener('DOMContentLoaded', () => {
        // fieldWeb: clear anything a password manager or browser autofilled before our ignore-hints applied.
        const webField = document.getElementById('field-website');
        if (webField) honeypot.value = '';
    });
    {% endif %}
</script>
